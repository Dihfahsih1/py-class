def splt():
    course = "Python,programming,course"
    # show_course = course.split(",", 0)
    # show_course = course.split(",", 1)
    show_course = course.split(",")
    print(show_course)
splt()

